#!/usr/bin/env perl6

use lib "{%*ENV<HOME>}/.shared-modules";
use ShopifyGit;

sub MAIN(Str $branch-name) {
  add-branch-to-fetch($branch-name);
  git(<<remote update>>);
  git(<<checkout $branch-name>>);
}
